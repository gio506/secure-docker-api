name: protected-prod-validation

on:
  push:
    branches:
      - prod
  pull_request:
    branches:
      - prod
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: secure-docker-api-prod-protected-${{ github.ref }}
  cancel-in-progress: false

jobs:
  approval-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: prod
    steps:
      - name: Protected branch approval
        run: echo "Prod validation approved. Starting protected checks."

  protected-checks:
    needs: approval-gate
    uses: ./.github/workflows/pr-validation.yml
    with:
      environment_name: prod
      env_file: .env.prod.example
      python_version: ${{ vars.PYTHON_VERSION || '3.11' }}
      app_env: ${{ vars.APP_ENV || 'prod' }}
      app_version: ${{ vars.APP_VERSION || '0.1.0' }}
      trivy_severity: ${{ vars.TRIVY_SEVERITY || 'CRITICAL,HIGH' }}
      app_host: ${{ vars.APP_HOST || '0.0.0.0' }}
      app_port: ${{ vars.APP_PORT || '8080' }}
      gunicorn_workers: ${{ vars.GUNICORN_WORKERS || '2' }}
      gunicorn_threads: ${{ vars.GUNICORN_THREADS || '2' }}
      gunicorn_timeout: ${{ vars.GUNICORN_TIMEOUT || '30' }}
